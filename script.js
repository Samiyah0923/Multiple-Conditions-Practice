$(".temperature-button").click(function() {
    var temperature = parseInt($(".temperature-input").val());
    
    if(temperature <= 50) {
        $(".output").text("Wear a winter jacket");
        
        else if(temperature<= 80)
        $().text()
    }
    // start your code here 
   
});